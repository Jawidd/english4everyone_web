FROM node:20-alpine AS build
WORKDIR /app

# Copy only package.json (no package-lock.json)
COPY package.json .

# Install dependencies (no cache layer trick needed)
RUN npm install

# Copy all source files
COPY . .

# Build the static site
RUN npm run build