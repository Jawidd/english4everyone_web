# Dockerfile.build — used ONLY by deploy.sh to build the static site
# Produces a /app/dist folder with the compiled React app

FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

RUN npm run build
